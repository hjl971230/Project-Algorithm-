#pragma once
#include<iostream>
#include<string>
#include<exception>
#include<limits>//numeric_limits 클래스(수치의 정밀도나 범위와 관련된 메서드를 가진 클래스)와 부동 소수점 표현 및 반올림과 관련된 다양한 메서드를 가진 파일
#include <vector>//STL 컨테이너중 하나인 vector 클래스를 사용
#include <algorithm>
#include <time.h>
#include <numeric>
#include <list>//STL 컨테이너중 하나인 list 클래스를 사용
#include <map>//STL 컨테이너중 하나인 list 클래스를 사용
#include <stack>
#include <queue>
#include <functional>
#include <fstream>
#include <Windows.h>
#include <conio.h>
using namespace std;